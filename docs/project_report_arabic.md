# تقرير شامل: نظام التعاون البحثي (Research Collaboration System)

## 📋 نظرة عامة على المشروع

هذا المشروع هو **نظام إدارة التعاون البحثي** متكامل يستخدم **أربع قواعد بيانات NoSQL مختلفة** للاستفادة من نقاط القوة الفريدة لكل نوع. المشروع مصمم لإدارة الباحثين، المشاريع البحثية، المنشورات، والعلاقات بينهم.

### الهدف الرئيسي
إنشاء نظام يوضح كيفية دمج قواعد بيانات NoSQL متعددة في تطبيق واحد، حيث تتخصص كل قاعدة بيانات في نوع معين من البيانات أو الاستعلامات.

---

## 🗄️ قواعد البيانات الأربعة

### 1. **MongoDB** - قاعدة البيانات الوثائقية
**الدور:** التخزين الأساسي للبيانات المنظمة

**ماذا تخزن:**
- ملفات الباحثين (الاسم، البريد الإلكتروني، القسم، المنصب)
- المشاريع البحثية (العنوان، الوصف، الحالة، التمويل)
- المنشورات (العنوان، المجلة، تاريخ النشر، الاستشهادات)
- حسابات المستخدمين (للدخول إلى النظام)

**لماذا MongoDB:**
- مرنة في تخزين البيانات المعقدة (JSON-like documents)
- تدعم استعلامات قوية ومتقدمة
- سهلة في التعديل والتوسع

**الملف المسؤول:** `code/repositories/mongo_repo.py`

---

### 2. **Neo4j** - قاعدة البيانات البيانية (Graph)
**الدور:** إدارة العلاقات والشبكات الاجتماعية

**ماذا تخزن:**
- علاقات التعاون بين الباحثين
- علاقات المشاركة في التأليف (Co-authorship)
- علاقات الإشراف (Supervisor-Student)
- علاقات الإرشاد (Mentorship)

**لماذا Neo4j:**
- متخصصة في تحليل الشبكات والعلاقات
- سريعة جداً في البحث عن المتعاونين والروابط
- تدعم استعلامات معقدة مثل "من يعرف من؟"

**الملف المسؤول:** `code/repositories/neo4j_repo.py`

---

### 3. **Redis** - قاعدة البيانات للتخزين المؤقت (Cache)
**الدور:** تسريع الأداء عبر التخزين المؤقت

**ماذا تخزن:**
- ملفات الباحثين المستخدمة بكثرة (في الذاكرة)
- إحصائيات سريعة (عدد المنشورات، H-index)
- جلسات المستخدمين (Sessions)
- بيانات مؤقتة للوصول السريع

**لماذا Redis:**
- سرعة فائقة (تعمل في الذاكرة RAM)
- استجابة في أقل من ميلي ثانية
- تقلل الحمل على قواعد البيانات الأخرى

**الملف المسؤول:** `code/repositories/redis_repo.py`

---

### 4. **Cassandra** - قاعدة البيانات للتحليلات
**الدور:** تخزين البيانات الزمنية والتحليلات

**ماذا تخزن:**
- مقاييس الأداء عبر الزمن
- إحصائيات الأقسام البحثية
- بيانات التحليلات والاتجاهات
- سجلات الأحداث (Event logs)

**لماذا Cassandra:**
- ممتازة للبيانات الكبيرة والسلاسل الزمنية
- قابلة للتوسع الأفقي بسهولة
- مصممة للكتابة السريعة

**الملف المسؤول:** `code/repositories/cassandra_repo.py`

---

## 💻 مكونات الكود الرئيسية

### 📁 المجلد: `code/`

#### 1. **database_manager.py** - مدير قواعد البيانات
**الوظيفة:** ينسق العمليات بين قواعد البيانات الأربعة

**ماذا يفعل:**
- يربط جميع قواعد البيانات عند بدء التشغيل
- ينشئ/يحدث/يحذف البيانات في جميع القواعد معاً
- يضمن تزامن البيانات بين القواعد المختلفة
- يدير العمليات المعقدة التي تشمل أكثر من قاعدة بيانات

**مثال:** عند إنشاء باحث جديد، يضيفه في MongoDB، ينشئ عقدة في Neo4j، ويخزنه مؤقتاً في Redis.

---

#### 2. **query_engine.py** - محرك الاستعلامات
**الوظيفة:** يوفر استعلامات متقدمة عبر قواعد البيانات

**ماذا يفعل:**
- البحث المتقدم عن الباحثين (حسب القسم، التخصص، H-index)
- تحليل شبكات التعاون
- إحصائيات الأقسام البحثية
- تحليل المنشورات والاتجاهات البحثية

**مثال:** يمكنه البحث عن "جميع الباحثين في قسم علوم الحاسوب الذين لديهم أكثر من 10 منشورات ويتعاونون مع قسم الرياضيات".

---

#### 3. **api_server.py** - خادم API
**الوظيفة:** يوفر واجهة برمجية (REST API) للنظام

**ماذا يفعل:**
- نقاط نهاية (Endpoints) لجميع العمليات
- مصادقة المستخدمين (JWT Tokens)
- إدارة الباحثين، المشاريع، المنشورات
- البحث والتحليلات

**التقنية:** FastAPI (إطار عمل Python سريع)

**مثال endpoint:**
```
GET /api/researchers/{id} - الحصول على ملف باحث
POST /api/researchers - إنشاء باحث جديد
GET /api/analytics/department/{dept_id} - إحصائيات قسم
```

---

#### 4. **web_interface.py** - الواجهة الويب
**الوظيفة:** واجهة مستخدم ويب كاملة

**ماذا يفعل:**
- صفحات تسجيل الدخول والتسجيل
- لوحة التحكم (Dashboard)
- إدارة الباحثين والمشاريع والمنشورات
- عرض التحليلات والإحصائيات
- تصور شبكات التعاون

**التقنية:** Flask (إطار عمل Python للويب)

**الصفحات الرئيسية:**
- `/login` - تسجيل الدخول
- `/dashboard` - لوحة التحكم
- `/researchers` - قائمة الباحثين
- `/projects` - المشاريع البحثية
- `/publications` - المنشورات
- `/analytics` - التحليلات

---

#### 5. **cli_interface.py** - واجهة سطر الأوامر
**الوظيفة:** أداة تفاعلية لإدارة النظام عبر Terminal

**ماذا يفعل:**
- إدارة الباحثين والمشاريع
- تشغيل الاستعلامات والتحليلات
- عرض الإحصائيات
- اختبار وظائف النظام

**الاستخدام:** `python code/cli_interface.py`

---

#### 6. **data_generator.py** - مولد البيانات التجريبية
**الوظيفة:** ينشئ بيانات وهمية واقعية للاختبار

**ماذا يفعل:**
- ينشئ باحثين وهميين بأسماء وبيانات واقعية
- يولد مشاريع ومنشورات عشوائية
- يضيف علاقات تعاون بين الباحثين
- يملأ قواعد البيانات ببيانات للتجربة

**الاستخدام:** `python code/data_generator.py`

---

#### 7. **auth_handler.py** - معالج المصادقة
**الوظيفة:** إدارة تسجيل الدخول والأمان

**ماذا يفعل:**
- تشفير كلمات المرور (Scrypt/Bcrypt)
- إنشاء والتحقق من JWT Tokens
- التحقق من صلاحيات المستخدمين

---

#### 8. **report_generator.py** - مولد التقارير
**الوظيفة:** إنشاء تقارير وتصدير البيانات

**ماذا يفعل:**
- تقارير PDF عن الباحثين والأقسام
- تصدير البيانات إلى Excel/CSV
- تقارير تحليلية مفصلة
- إحصائيات مرئية

---

#### 9. **rbac.py** - التحكم في الصلاحيات
**الوظيفة:** إدارة أدوار المستخدمين وصلاحياتهم

**الأدوار:**
- `admin` - مدير النظام (كل الصلاحيات)
- `researcher` - باحث (تعديل ملفه فقط)
- `viewer` - مشاهد (قراءة فقط)

---

#### 10. **logging_config.py** - إعدادات السجلات
**الوظيفة:** تسجيل جميع الأحداث والأخطاء

**ماذا يفعل:**
- يسجل جميع العمليات
- يتتبع الأخطاء والمشاكل
- يساعد في تشخيص المشاكل

---

## 🎨 قوالب HTML (المجلد: `code/templates/`)

يحتوي على **21 صفحة HTML** للواجهة:

### الصفحات الرئيسية:
- `base.html` - القالب الأساسي لجميع الصفحات
- `index.html` - الصفحة الرئيسية
- `login.html` - تسجيل الدخول
- `signup.html` - إنشاء حساب جديد
- `dashboard.html` - لوحة التحكم

### صفحات الباحثين:
- `researchers.html` - قائمة الباحثين
- `researcher_detail.html` - تفاصيل باحث
- `compare_researchers.html` - مقارنة باحثين

### صفحات المشاريع:
- `projects.html` - قائمة المشاريع
- `project_detail.html` - تفاصيل مشروع

### صفحات المنشورات:
- `publications.html` - قائمة المنشورات
- `publication_detail.html` - تفاصيل منشور

### صفحات التحليلات:
- `analytics.html` - التحليلات والإحصائيات
- `reports.html` - التقارير
- `network_visualization.html` - تصور شبكات التعاون

### صفحات أخرى:
- `profile.html` - الملف الشخصي
- `settings.html` - الإعدادات
- `activity_status.html` - حالة قواعد البيانات
- `advanced_search.html` - البحث المتقدم

---

## 🐳 Docker والبنية التحتية

### **docker-compose.yml** - ملف التشغيل الرئيسي
**الوظيفة:** يشغل جميع الخدمات معاً

**الخدمات التي يشغلها:**
1. **mongodb** - قاعدة بيانات MongoDB
2. **neo4j** - قاعدة بيانات Neo4j
3. **redis** - قاعدة بيانات Redis
4. **cassandra** - قاعدة بيانات Cassandra
5. **research-api** - خادم API
6. **research-web** - خادم الويب
7. **prometheus** - مراقبة الأداء
8. **grafana** - لوحات تحكم الأداء

**كيفية التشغيل:**
```bash
docker-compose up -d
```

**الوصول للخدمات:**
- الواجهة الويب: `http://localhost:3000`
- API: `http://localhost:8000`
- Neo4j Browser: `http://localhost:7474`
- Grafana: `http://localhost:3001`

---

### **Dockerfile** - ملف بناء التطبيق
**الوظيفة:** يحدد كيفية بناء صورة Docker للتطبيق

**ماذا يفعل:**
- يثبت Python 3.11
- يثبت جميع المكتبات المطلوبة
- ينسخ الكود إلى الحاوية
- يجهز البيئة للتشغيل

---

### **requirements.txt** - المكتبات المطلوبة
**الوظيفة:** قائمة بجميع مكتبات Python المطلوبة

**أهم المكتبات:**
- `pymongo` - للتعامل مع MongoDB
- `neo4j` - للتعامل مع Neo4j
- `redis` - للتعامل مع Redis
- `cassandra-driver` - للتعامل مع Cassandra
- `fastapi` - لبناء API
- `flask` - لبناء الواجهة الويب
- `passlib` - لتشفير كلمات المرور
- `pandas` - لتحليل البيانات

---

## 📚 التوثيق (المجلد: `docs/`)

يحتوي على **8 ملفات توثيق شاملة:**

### 1. **00_project_summary.md**
ملخص شامل للمشروع وأهدافه وإنجازاته

### 2. **01_requirement_analysis.md**
تحليل المتطلبات ومبررات اختيار قواعد البيانات

### 3. **02_system_architecture.md**
معمارية النظام وتصميمه

### 4. **03_data_modeling.md**
نماذج البيانات والمخططات (Schemas)

### 5. **04_design_analysis.md**
تحليل القرارات التصميمية والمقايضات

### 6. **05_technical_implementation.md**
دليل التنفيذ الفني والإعداد

### 7. **AGILE_PROJECT_MANAGEMENT.md**
إدارة المشروع بمنهجية Agile

### 8. **SE_COMPLIANCE_CHECKLIST.md**
قائمة التحقق من معايير هندسة البرمجيات

---

## 🧪 الاختبارات (المجلد: `tests/`)

### أنواع الاختبارات:

#### 1. **Unit Tests** (اختبارات الوحدة)
- `test_mongo_repo.py` - اختبار MongoDB
- `test_neo4j_repo.py` - اختبار Neo4j
- `test_redis_repo.py` - اختبار Redis

#### 2. **Integration Tests** (اختبارات التكامل)
- اختبار التكامل بين قواعد البيانات

#### 3. **E2E Tests** (اختبارات شاملة)
- اختبار النظام من البداية للنهاية

**تشغيل الاختبارات:**
```bash
pytest tests/
```

---

## 🔧 ملفات الإعداد (المجلد: `setup/`)

### 1. **setup/mongodb/**
- `init-mongo.js` - سكريبت تهيئة MongoDB

### 2. **setup/neo4j/**
- `init-neo4j.cypher` - سكريبت تهيئة Neo4j

### 3. **setup/cassandra/**
- `init-cassandra.cql` - سكريبت تهيئة Cassandra

### 4. **setup/redis/**
- `redis.conf` - إعدادات Redis

---

## 🔐 ملف البيئة (`.env`)

**الوظيفة:** يحتوي على إعدادات الاتصال بقواعد البيانات

**المحتوى:**
```
MONGODB_URI=mongodb://admin:research_admin_2024@localhost:27017/...
NEO4J_URI=bolt://localhost:7687
NEO4J_PASSWORD=research_neo4j_2024
REDIS_URL=redis://:research_redis_2024@localhost:6379
CASSANDRA_HOST=localhost
```

---

## 📊 المراقبة والأداء

### **Prometheus** (المجلد: `docker/prometheus/`)
- يراقب أداء النظام
- يجمع المقاييس والإحصائيات
- يتتبع استخدام الموارد

### **Grafana**
- لوحات تحكم مرئية
- رسوم بيانية للأداء
- تنبيهات عند المشاكل

---

## 🚀 كيفية تشغيل المشروع

### الطريقة 1: باستخدام Docker (الأسهل)
```bash
# تشغيل جميع الخدمات
docker-compose up -d

# الوصول للواجهة
http://localhost:3000
```

### الطريقة 2: محلياً
```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل الواجهة الويب
python code/web_interface.py

# تشغيل API
python code/api_server.py
```

---

## 🎯 حالات الاستخدام الرئيسية

### 1. **إدارة الباحثين**
- إضافة باحث جديد
- تحديث معلومات الباحث
- البحث عن باحثين
- عرض شبكة التعاون

### 2. **إدارة المشاريع**
- إنشاء مشروع بحثي
- إضافة باحثين للمشروع
- تتبع حالة المشروع
- إدارة التمويل

### 3. **إدارة المنشورات**
- إضافة منشور جديد
- ربط المنشور بالباحثين
- تتبع الاستشهادات
- تحليل التأثير البحثي

### 4. **التحليلات**
- إحصائيات الأقسام
- تحليل شبكات التعاون
- اتجاهات البحث
- تقارير الأداء

---

## 🔑 معلومات تسجيل الدخول

### حسابك الحالي:
- **البريد الإلكتروني:** `basharwazwaz@gmail.com`
- **كلمة المرور:** `123456`

(تم إعادة تعيين كلمة المرور كما طلبت)

---

## 📈 الإحصائيات والأداء

### نقاط القوة:
- ✅ معدل نجاح التخزين المؤقت: 90%+
- ✅ زمن الاستجابة: أقل من 100 ميلي ثانية
- ✅ تحسين الأداء: 3-5 أضعاف مقارنة بقاعدة بيانات واحدة
- ✅ قابلية التوسع: كل قاعدة بيانات مستقلة

---

## 🛠️ الملفات المساعدة

### ملفات التكوين:
- `pyproject.toml` - إعدادات المشروع
- `pytest.ini` - إعدادات الاختبارات
- `.gitmessage` - قالب رسائل Git
- `.githooks/` - Git hooks للجودة

### ملفات CI/CD:
- `.github/workflows/ci.yml` - التكامل المستمر
- `.github/ISSUE_TEMPLATE/` - قوالب المشاكل

---

## 🎓 المفاهيم التقنية المستخدمة

### 1. **Polyglot Persistence**
استخدام أنواع مختلفة من قواعد البيانات في نفس التطبيق

### 2. **Event-Driven Architecture**
التزامن بين قواعد البيانات عبر الأحداث

### 3. **Caching Strategy**
استخدام Redis لتسريع الوصول للبيانات

### 4. **Repository Pattern**
فصل منطق الوصول لقواعد البيانات

### 5. **Facade Pattern**
واجهة موحدة للتعامل مع قواعد البيانات المتعددة

---

## 🔍 ملخص سريع

### ما هو هذا المشروع؟
نظام لإدارة التعاون البحثي يستخدم 4 قواعد بيانات NoSQL مختلفة

### لماذا 4 قواعد بيانات؟
كل قاعدة متخصصة في نوع معين من البيانات:
- MongoDB → البيانات المنظمة
- Neo4j → العلاقات والشبكات
- Redis → التخزين المؤقت السريع
- Cassandra → التحليلات والبيانات الزمنية

### كيف أستخدمه؟
1. شغل Docker: `docker-compose up -d`
2. افتح المتصفح: `http://localhost:3000`
3. سجل دخول بحسابك

### ما الملفات المهمة؟
- `database_manager.py` - القلب النابض للنظام
- `web_interface.py` - الواجهة الويب
- `api_server.py` - API للتطبيقات الخارجية
- `docker-compose.yml` - لتشغيل كل شيء

---

## 📞 المساعدة والدعم

إذا واجهت أي مشكلة:
1. تحقق من تشغيل Docker: `docker ps`
2. راجع السجلات: `docker-compose logs`
3. تأكد من تشغيل جميع الخدمات

---

**تاريخ التقرير:** 26 يناير 2026  
**حالة المشروع:** ✅ جاهز للاستخدام  
**الإصدار:** 1.0.0
